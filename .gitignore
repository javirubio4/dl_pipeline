.venv/
.ipynb_checkpoints/
__pycache__/
*.pkl
*.parquet
*.csv
.DS_Store
model_weights.torch
outputs/
dl_pipeline/model_weights.torch
scripts/submit_slurm.sh

# Patient mapping files contain re-identification keys — never commit
configs/id_mapping_*.csv

# Patient ID lists (whitelists) — also sensitive
configs/*_patient_ids.csv